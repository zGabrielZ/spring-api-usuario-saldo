spring:
  datasource:
    url: jdbc:postgresql://localhost:5000/usuario_saldos?createDatabaseIfNotExist=true
    username: gabriel
    password: gabriel
  jpa:
    hibernate:
      ddl-auto: none
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: dbo
  flyway:
    enabled: true
    baselineOnMigrate: true
    schemas:
      - dbo
cache:
  type: redis
redis:
  host: localhost
  port: 6379